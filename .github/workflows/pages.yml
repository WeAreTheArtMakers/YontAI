# GitHub Pages deployment is configured manually:
# Settings → Pages → Deploy from branch → main → / (root)
# This workflow file is kept for reference only.
name: placeholder
on: push
jobs:
  placeholder:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Use Settings → Pages → Deploy from branch → main → / (root) instead"